# TF-78-04 evidence — Claude Code smoke test

Real artifact, not a transcript. Generated by speaking the MCP stdio protocol directly to the
locally-installed plugin binary (`node .../cache/hermetic-diagrams/hermetic-diagrams/0.3.1/dist/cli/bin.js serve`)
via [`mcp-evidence-client.mjs`](mcp-evidence-client.mjs) in this same directory — `initialize` →
`tools/call containment_status` → `tools/call render_diagram`. The PNG in this folder
(`tf-78-04-claude-code-smoke-test.png`) is the base64 `image` content block from that
`render_diagram` response, decoded to disk, with no manual editing.

- **When:** 2026-09-23T03:32:07Z
- **Installed version:** `@scrapup/hermetic-diagrams@0.3.1` (plugin cache path:
  `~/.claude/plugins/cache/hermetic-diagrams/hermetic-diagrams/0.3.1`), installed via
  `/plugin marketplace add scrapup/hermetic-diagrams` → `/plugin install hermetic-diagrams` in
  Claude Code, with no manual `npm run build` step — the `dist/` this binary shipped with came
  from the `dist` branch (TF-78-01/PR #7).
- **`containment_status` response** (raw, from the JSON-RPC `tools/call` result):
  ```json
  {
    "contained": true,
    "checks": {
      "krokiHealth": "pass",
      "egressSelfCheck": "pass",
      "canaryRender": "pass",
      "krokiSafeMode": "SECURE",
      "publishedPorts": "none"
    }
  }
  ```
- **`render_diagram` call:** `format: plantuml`, `output: png`, source rendering the sequence
  shown in the PNG. Boot log confirmed by the MCP process on stderr during this run:
  `{"level":"info","event":"boot.containment","contained":true,"krokiHealth":"pass","egressSelfCheck":"pass","canaryRender":"pass","krokiSafeMode":"SECURE"}`
  and `{"level":"info","event":"render.ok","format":"plantuml","output":"png","durationMs":27}`.

## Reproduce

```bash
HERMETIC_DIAGRAMS_BIN=~/.claude/plugins/cache/hermetic-diagrams/hermetic-diagrams/<version>/dist/cli/bin.js \
  node docs/specs/plugin-distribution/evidence/mcp-evidence-client.mjs
```

Requires the plugin already installed locally via `/plugin install hermetic-diagrams` — point
`HERMETIC_DIAGRAMS_BIN` at the currently-installed version's `dist/cli/bin.js`.
